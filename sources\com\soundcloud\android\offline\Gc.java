package com.soundcloud.android.offline;

import java.util.concurrent.TimeUnit;

/* compiled from: OfflineConstants */
class Gc {
    static final long a = TimeUnit.SECONDS.toMillis(15);
    static final long b = TimeUnit.SECONDS.toMillis(1);

    Gc() {
    }
}
